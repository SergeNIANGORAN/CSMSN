
  model {
      for( i in 1 : ny ) {
      y[i] ~ dbin(p[i],n[i])
      p[i] ~ dbeta(alpha,beta)
      }
      alpha ~ dunif(0,1e3)
      beta ~ dunif(0,1e3)
      #alpha ~ dgamma(0.001,0.001)
      #beta ~ dgamma(0.001,0.001)
  }

